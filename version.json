{
  "version": "1.1.35",
  "notes": "Auto-update repeat prompt fix. App now keeps the current version in the bundled HTML so the same update is not requested again after every close and reopen. Native auto-update remains enabled."
}
